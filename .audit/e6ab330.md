# Post-Merge Audit: e6ab330

**Date:** 2026-08-21
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `e6ab330` — roadmap: task 10 -> in_progress

## Range reviewed

| Task | Delivery and review commits | Surface |
|------|-----------------------------|---------|
| 11 | `6943c95` | Split mixed internet/mock-server suites so `:external_network` and `:local_network` match the socket |
| 9 | `f3d58fb` | Extract `HeartbeatInterval`, `SafeCallback`, and a single `Client.build_client_struct/2` |

Roadmap-only commits in the supplied range (`ffecf44`, `3f50137`, `fcd823d`, `8bfe9f0`, `e6ab330`) were checked for continuity and left unchanged.

## What I reviewed

- Task 9: `build_client_struct/2` is the one constructor; `reconnect_opts_from_state/1` is private; both heartbeat pong paths call `HeartbeatInterval.record_pong/3`; both lifecycle callbacks call `SafeCallback.invoke/2`. CLAUDE.md, USAGE_RULES.md, and the validate-usage allow-list already name `build_client_struct/2`. New modules have `@moduledoc`, `@spec`, and unit tests. Doctor reports 100% docs/specs on both.
- Task 11: per-describe tags in its write-set match the socket. Unit tests that open no socket (invalid URL validation, default-handler ignore, dead-pid `send_balanced/2`) were untagged so they run in default `mix test`. The dead-pid assertion changed from `{:not_connected, :process_down}` to `:no_connections`; that matches `send_balanced/2` filtering through `alive_client_pid?/1` before `PoolRouter.select_connection/1`.
- Hygiene scan of landed additions: no leftover `IO.inspect`/`dbg`, no `@tag :skip`, no new `# credo:disable`.
- No reviewer rejections were recorded, so there is no false-rejection note.

## Findings

| # | Category | Description | Resolution |
|---|----------|-------------|------------|
| 1 | committed-ephemeral | Task 9 committed the harness-injected `AGENTS.md` prefix (`<!-- harness-injected: canonical agent rules — ephemeral, do not commit -->`). That file is in the Hex package and hexdocs extras | **fixed** |
| 2 | changelog-gap | `[Unreleased]` had no entry for the constructor/export change or the two shared helpers | **fixed** |
| 3 | stale-docs | `docs/Architecture.md` still omitted `HeartbeatInterval` and `SafeCallback` | **fixed** |
| 4 | leftover mixed tags | Task 11's `out_of_scope` listed `error_integration_test` and `client_supervisor_test` as already dual-tagged; they are not. `error_handling_test` and `supervised_client_test` were never in the write-set and still mix mock-server with invalid-host dials under `@moduletag :integration` | **filed as task 12** |

## What I fixed

- Stripped the ephemeral harness prefix from `AGENTS.md` so it again matches the CLAUDE.md render (`mix agents.check`: `OK: ./AGENTS.md is up to date`).
- Added an `[Unreleased]` note for `build_client_struct/2`, private `reconnect_opts_from_state/1`, `HeartbeatInterval`, and `SafeCallback`.
- Documented the two new infrastructure modules in `docs/Architecture.md`.

## Discoveries filed

**Task 12** — Finish dual-tagging the suites task 11 left mixed or `:integration-only`. Evidence in the task body: `error_integration_test.exs` still traps `ErrorHandler` unit tests under `:integration` and mixes Deribit internet calls; `examples/error_handling_test.exs` and `examples/supervised_client_test.exs` mix MockWebSockServer with invalid-host dials; `client_supervisor_test.exs` is `:external_network` only.

## Cold check

Command: `mix deps.get` then `mix check.dispatch` in this intentionally cold worktree — **passed** (exit 0).

Witness: 522 tests passed, 0 failed, 92 excluded; coverage 76.48% (threshold 58); credo strict clean (91 files, no issues); doctor passed (HeartbeatInterval and SafeCallback at 100% docs/specs); ex_dna no clones; reach arch/smells clean; sobelow skip-clean; deps.audit clean; dialyzer passed (PLT built from scratch); `agents.check` OK. Also recorded in `.harness/audit.json` `cold_check`.

## Verification (audit fixes)

- Docs, changelog, and AGENTS.md prefix strip. No behaviour change.
- Cold `mix check.dispatch` is the suite witness for the already-landed code range.

## Clean areas

- The three task-9 extractions are real shared helpers, not wrappers. `HeartbeatInterval.record_pong/3` lets Deribit accumulate types while generic pongs replace the set.
- Task 11's write-set tagging is accurate; untagged unit tests in those files no longer ride a network tag.
- No roadmap status edits in this audit commit; no reverts.
