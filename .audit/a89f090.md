# Post-Merge Audit: a89f090

**Date:** 2026-08-21
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `a89f090` — roadmap: task 3 -> done (shipped 3dfd9eb44d65)

## Range reviewed

| Commit | Subject |
|--------|---------|
| `f34a2b8` | **code** harness delivery — task 5 heartbeat/pool health metrics |
| `688d2ef` | roadmap: task 3 -> in_progress |
| `bae2fb2` | roadmap: task 5 -> done (shipped f34a2b8) |
| `784200c` | roadmap: task 6 -> in_progress |
| `3dfd9eb` | **code** harness delivery — task 3 RateLimiter token race and discarded-request queue |
| `a89f090` | roadmap: task 3 -> done (shipped 3dfd9eb) |

**Code-bearing surface:** `lib/zen_websocket/rate_limiter.ex`, `lib/zen_websocket/heartbeat_manager.ex`, `lib/zen_websocket/pool_router.ex`, `lib/zen_websocket/client.ex`, `lib/zen_websocket/message_handler.ex`, `lib/zen_websocket/reconnection.ex`, plus tests for rate limiter, heartbeat, pool router, Deribit helper, and mix project. Roadmap-only commits ignored for source edits.

## What I reviewed

- **Task 3 (`3dfd9eb`):** RateLimiter consume/refill now compare-and-swap via `:ets.select_replace/2`. The request queue is gone; `consume/2` is `:ok | {:error, :rate_limited}`. `status/1` keeps `queue_size` / `pressure_level` / `suggested_delay_ms` at neutral values for response compatibility. Concurrency regression and “no tokens spent on unsent requests” tests are present.
- **Task 5 (`f34a2b8`):** `:ping_pong` sends a unique payload, correlates the matching pong, increments `heartbeat_failures` on a missed pong, and emits RTT telemetry. `silence_pings: false` on Gun upgrade so pongs reach the client; MessageHandler no longer double-pongs. Pool ETS table is owned by a dedicated process. Deribit helper heartbeat path has tests.
- Hygiene scan: no leftover `IO.inspect`/`dbg`, no `@tag :skip`, no new `# credo:disable`. Heartbeat Logger.info emoji lines predate this range.
- **Reviewer rejections:** none recorded for this project. No false-rejection note.

## Findings

| # | Category | Description | Resolution |
|---|----------|-------------|------------|
| 1 | changelog-gap | `[Unreleased]` had no entry for the two landed deliveries | **fixed** |
| 2 | stale-docs | USAGE_RULES, performance_tuning, and Architecture still documented `max_queue_size`, `:queue_full`, queue telemetry, and live pressure/backpressure after the queue was removed | **fixed** |
| 3 | leftover-constant | Pool health still scores `pressure_level` that a real `Client.get_state_metrics/1` never sets (always `:none`). Task 5’s non-zero penalty test stubs the key on `FakeMetricsClient`. RateLimiter.status/1 also hardcodes `:none` after task 3 | **filed as task 10**; moduledoc now says the key is optional and absent keys score `:none` |
| 4 | convention | Bare `TODO:` in `pool_router.ex` `calculate_health/1` @doc (project rule is `TODO(Task N)` or no TODO) | **fixed** (turned into an explanatory sentence) |

Heartbeat ping/pong correlation and the RateLimiter CAS look sound. The pool-table owner process is a real fix. The pressure “fix” in task 5 is a false green on the production Client path; that is the only leftover defect, and it is a design choice (new source vs drop the term), not an inline doc patch.

## What I fixed

- CHANGELOG Unreleased entries for both landings.
- USAGE_RULES.md, `docs/guides/performance_tuning.md`, and `docs/Architecture.md` now describe RateLimiter as an allow/deny gate.
- `PoolRouter` moduledoc: pressure is optional and defaults to `:none`.
- `Client` frame-handler comment: Gun already answered inbound pings.
- Replaced the bare TODO in `calculate_health/1`.

## Discoveries filed

**Task 10** — Give pool health a real pressure signal or drop the dead penalty. Evidence in the task body: `Client.get_state_metrics/1` has no `:pressure_level`, `RateLimiter.status/1` is permanently `:none`, and the task-5 test injects the key on a fake client.

## Cold check

Command: `mix deps.get` then `mix check.dispatch` in this intentionally cold worktree — **passed** (exit 0).

Witness: 412 tests passed, 0 failed, 160 excluded; coverage 61.1% (threshold 58); credo strict clean; doctor passed; ex_dna no clones; reach arch/smells clean; sobelow skip-clean; deps.audit clean; dialyzer passed (PLT built from scratch); `agents.check` OK. Also recorded in `.harness/audit.json` `cold_check`.

## Verification (audit fixes)

- Docs, changelog, and two comment/moduledoc edits. No behaviour change.
- Cold `mix check.dispatch` is the suite witness for the already-landed code range.

## Clean areas

- RateLimiter token accounting is now a CAS loop; the discarded-request queue is gone rather than half-wired.
- Heartbeat failure_count can actually rise; pong RTT is measured from the ping that was sent.
- Pool ETS no longer dies with the first caller.
- No roadmap status edits in this audit commit; no reverts.
