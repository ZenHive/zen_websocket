# Post-Merge Audit: 74c9bd5

**Date:** 2026-08-21
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `74c9bd5` — roadmap: file task 15 to fix the validate_usage regex that forced flat Client module names

## Range reviewed

| Task | Delivery and review commits | Surface |
|------|-----------------------------|---------|
| 12 | `63089b8`, `9ad7377` | Dual-tag mixed/integration-only suites; NetworkTagGuard scoped per test |
| R056 | `f99e505`, `9d72056` | Coverage gate measures core library only (atoms + Mix regexes; floor 90) |
| 13 | `3296789` | NetworkTagGuard: over-tagging, raw listeners, local sockets that are not MockWebSockServer |
| 14 | `1c1b092`, `12c7a53` | Decompose Client into responsibility-scoped `Client*` modules |

Roadmap-only commits in the supplied range (`e99a9c7`, `d54b065`, `a4aa46b`, `bf5519d`, `0754c76`, `06f4460`, `cda045f`, `72ac46a`, `74c9bd5`) were checked for continuity and left unchanged.

## What I reviewed

- Task 12/13: NetworkTagGuard now follows module/describe/test tag scope. The mistagged fixture is `.fixture` (not compiled), so the reviewer correctly dropped the Credo `test/fixtures/` exclude. The suite scan is empty. Raw `:gen_tcp.listen` / `:ssl.listen` / `:gen_udp.open` count as local sockets. Over-tagging (`:external_network` without an internet socket, `:integration` without a network socket) reds.
- Task R056: `ignore_modules` keeps Mix regexes and matching atoms; `mix_project_test.exs` asserts completeness in both directions. Floor is `@core_cover_threshold 90`. CLAUDE.md / AGENTS.md already describe the ratchet.
- Task 14: `Client` is 473 lines (was 1227). GenServer callbacks stay on `Client`; ten `Client*` modules hold call wrapping, Gun lifecycle, retry, frames, correlation, recording, and callback bodies. Public API unchanged. Architecture.md, CLAUDE.md, and `ZenWebsocket` key-modules text list the split. Doctor: 100% docs/specs on every new module. New modules stay within the 5-public-function cap.
- Hygiene scan of landed additions: no leftover `IO.inspect`/`dbg`, no `@tag :skip`, no new `# credo:disable`, no bare `TODO:`. AGENTS.md has no harness-injected prefix.
- No reviewer rejections were recorded, so there is no false-rejection note. Task 14 was approved after the reviewer flattened nested `Client.*` names to dodge `validate_usage`; that outcome is already filed as task 15.

## Findings

| # | Category | Description | Resolution |
|---|----------|-------------|------------|
| 1 | changelog-gap | `[Unreleased]` had no entry for the Client split or the core-only coverage ratchet (58 → 90) | **fixed** |
| 2 | stale-comment | `.credo.exs` still named `client.ex handle_info/2 :gun_ws` as a complexity-11 site; that callback is now a one-line delegate | **fixed** |

The flat `ZenWebsocket.ClientCallFacade` (etc.) names and the `~r/ZenWebsocket\.Client\.(\w+)/` function-name regex are real defects. They were filed as **task 15** at the end of this range; this audit does not re-file or implement that rename.

## What I fixed

- Added `[Unreleased]` notes for the Client decomposition and the core-only coverage floor.
- Pointed the Credo cyclomatic-complexity comment at `Config.validate/1` and `ClientFrames.handle_ws/4`.

## Discoveries filed

None. Task 15 already covers the validate_usage regex and the nested `Client.*` rename.

## Cold check

Command: `mix deps.get` then `mix check.dispatch` in this intentionally cold worktree — **passed** (exit 0).

Witness: 597 tests passed, 0 failed, 70 excluded; coverage 94.23% (threshold 90); credo strict clean (108 files, no issues); doctor passed (all new `Client*` modules at 100% docs/specs); ex_dna no clones; reach arch/smells clean; sobelow skip-clean; deps.audit clean; dialyzer passed (PLT built from scratch); `agents.check` OK. Also recorded in `.harness/audit.json` `cold_check`.

## Verification (audit fixes)

- Changelog and a Credo comment. No behaviour change.
- Cold `mix check.dispatch` is the suite witness for the already-landed code range.

## Clean areas

- Coverage ignore-list completeness is tested, not hand-waved; extras that do not match a Mix regex fail the gate.
- NetworkTagGuard is now a per-test class check with a red fixture, not a one-suite special case.
- The Client split keeps Gun ownership on the Client process; retry attempt-identity timers stay in `ClientConnection`.
- No roadmap status edits in this audit commit; no reverts.
