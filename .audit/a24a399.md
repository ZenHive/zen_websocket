# Post-merge audit: a24a399

**Date:** 2026-09-20
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `a24a399` — audit(37db6c1): report clean plugin-settings range and file QA follow-ups
**Integrated revision:** `a24a399dc227ab24755738fa9ba31b7933e0a176`

Reviewed `a745850..a24a399` (two commits). Hygiene of the range is clean. Full-project QA at the integrated revision **failed**: `mix test.json` confirmed one `:local_network` failure that is the MockWebSockServer handler race already filed as task 22. Two freshness gates still skip on this host (task 23). No new tasks. No source edits.

## Range reviewed

| Commit | Subject | Surface |
|--------|---------|---------|
| `37db6c1` | chore: remove uninstalled ZenHive plugin references | `.claude/settings.json` |
| `a24a399` | audit(37db6c1): report clean plugin-settings range and file QA follow-ups | `.audit/37db6c1.md`, `roadmap/tasks.toml`, `roadmap/data.json`, `ROADMAP.md` |

No library, test, changelog, or docs files moved. No reviewer rejections were recorded for this project, so there is no false-rejection note.

## What I reviewed

- `37db6c1` emptied `enabledPlugins` and left `extraKnownMarketplaces.zenhive` pointing at `ZenHive/claude-marketplace` with `autoUpdate: true`. That split is still coherent: plugins are off at project level; the marketplace stays discoverable. Empty `"enabledPlugins": {}` is an explicit disable, not dead JSON. Remaining "ZenHive" strings in mix.exs, README, LICENSE, CHANGELOG, and docs are the GitHub org, not plugin enables. No CHANGELOG gap (local Claude Code config, not a Hex-package change).
- `a24a399` is the prior audit's report plus rmap filings for tasks 22 and 23. Both tasks are still `pending`, routed to `cursor` / `cursor-grok-4.6-high`, and match the defects this QA run reproduced. `rmap render` also rewrote historical efficiency scores with a trailing `?` and changed "Last shipped" to "no recent shipments" (Task 21 shipped 2026-08-25, outside rmap's recency window). That is renderer noise, not a library defect; left unchanged.
- No leftover `IO.inspect` / `dbg` / `@tag :skip` / bare `TODO:` in the landed diffs.

## Findings

None in the landed range. No source edits.

## Discoveries filed

None this audit. QA failures semantically match already-pending tasks:

**Task 22** (already pending) — Close MockWebSockServer's async handler and connection-registration race. This cold run confirmed it as a hard fail, not only a healed flake. `client_test.exs:497` received `{:handler_received, "echo: trigger"}` against a custom subscription-JSON handler. Retry also healed `client_test.exs:108` (`"echo: Custom handler test"`) and `client_test.exs:649` (JSON-RPC `send_message` timed out waiting for a correlated reply). Same mechanism as the prior audit: Cowboy starts with `handler: nil`; `websocket_init/1` registers asynchronously; `set_handler/2` only notifies connections already in `state.connections`. Substantial; not implemented here.

**Task 23** (already pending) — Prove AGENTS.md and advisory-mirror freshness without developer-host paths. `host_script/1` skipped both gates again (`~/_DATA/code/onchain-stack/bin/advisory-freshness.sh` and `~/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh` absent), then `mix deps.audit` printed "No vulnerabilities found." Substantial; not implemented here.

## Full-project QA

Command: `mix deps.get` then `mix precommit.full` at `a24a399dc227ab24755738fa9ba31b7933e0a176` on a cold worktree (no `deps/` / `_build/` at start). Alias exit 1. Status: **failed** — `test.json` confirmed 1 failure (same race as task 22). Freshness gates also skipped (task 23). Because the alias aborted at tests, `MIX_ENV=dev mix dialyzer` and `mix agents.check` were collected independently afterward.

| Step | Outcome |
|------|---------|
| `compile --warnings-as-errors` | passed (`Generated zen_websocket app`, 49 files) |
| `format --check-formatted` | passed (silent) |
| `credo --strict` | passed — 113 files, 917 mods/funs, no issues |
| `doctor --raise` | passed — 50 modules, 100% doc / moduledoc / spec coverage |
| `ex_dna --max-clones 0` | passed — no duplication, 49 files, budget 0/0 |
| `reach.check --arch --smells` | passed — architecture OK, no smells |
| `sobelow --skip --exit low` | passed — SCAN COMPLETE (no-router warning expected; not Phoenix) |
| `deps.audit.gated` | **incomplete** — advisory-mirror freshness skipped; then "No vulnerabilities found." |
| `test.json --cover --cover-threshold 90 --exclude integration --include local_network` | **failed** — 641 passed, 1 failed, 2 flaky, 70 excluded; retry `confirmed: 1`, `flaky: 2`; coverage 94.14% (threshold 90, met) |
| `MIX_ENV=dev mix dialyzer` | passed (independent; alias never reached it) — PLT built from scratch; total errors 1, skipped 1 |
| `agents.check` | **incomplete** (independent) — AGENTS.md freshness skipped |

Logs: `/tmp/harness-precommit-full.ybw3Vz.log`, `/tmp/harness-remaining-checks.ybw3Vz.log`. Hex `mix deps.get` also printed published advisories on transitive packages; those were not mixed into git. Repair for the race is task 22; repair for the unproven audit scan is task 23.

## Clean areas

- Plugin enables remain gone; no stray `elixir@zenhive` / `harness@zenhive` references.
- Tasks 22 and 23 were not re-filed; no roadmap status edits.
- No reverts.
