# Post-merge audit: 37db6c1

**Date:** 2026-09-20
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `37db6c1` — chore: remove uninstalled ZenHive plugin references
**Integrated revision:** `37db6c170bbb7056f9bf5480de422fa2798c0989`

Reviewed `a745850..37db6c1` (one commit). Hygiene of that commit is clean. Full-project QA at the integrated revision completed with two host-script skips and three healed local_network flakes; both issues are filed, not patched here.

## Range reviewed

| Commit | Subject | Surface |
|--------|---------|---------|
| `37db6c1` | chore: remove uninstalled ZenHive plugin references | `.claude/settings.json` |

No library, test, changelog, or docs files moved. No reviewer rejections were recorded for this project, so there is no false-rejection note.

## What I reviewed

- The commit emptied `enabledPlugins` (`elixir@zenhive`, `elixir-workflows@zenhive`, `harness@zenhive`) and left `extraKnownMarketplaces.zenhive` pointing at `ZenHive/claude-marketplace` with `autoUpdate: true`. That split is coherent: plugins are off at project level; the marketplace stays discoverable. Remaining "ZenHive" strings in mix.exs, README, LICENSE, CHANGELOG, and docs are the GitHub org that owns this repo, not plugin enables.
- Empty `"enabledPlugins": {}` is an explicit project-level disable, not dead JSON.
- No CHANGELOG gap: this is local Claude Code config, not a Hex-package change.
- No leftover `IO.inspect` / `dbg` / `@tag :skip` / bare `TODO:` in the landed diff (the diff is JSON only).
- `CLAUDE.md` / `AGENTS.md` were not part of this commit.

## Findings

None in the landed range. No source edits.

## Discoveries filed

**Task 22** — Close MockWebSockServer's async handler and connection-registration race. Cold `mix precommit.full` passed only after ex_unit_json retried three `:local_network` failures (`confirmed: 0`, `flaky: 3`). `basic_usage_test.exs` received `echo: test message` / `echo: first` against a custom exact-echo handler; `testing_test.exs` `inject_message/2` timed out with an empty mailbox. Cowboy starts with `handler: nil`; `websocket_init/1` registers asynchronously; `set_handler/2` only notifies connections already in `state.connections`. Substantial; not implemented in this audit.

**Task 23** — Prove AGENTS.md and advisory-mirror freshness without developer-host paths. `host_script/1` skipped both gates because `~/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh` and `~/_DATA/code/onchain-stack/bin/advisory-freshness.sh` are absent on this harness node, then `mix deps.audit` printed "No vulnerabilities found." That is the false-green `mix_audit` sync-status hole the gated alias exists to close. Substantial; not implemented in this audit.

## Full-project QA

Command: `mix deps.get` then `mix precommit.full` at `37db6c170bbb7056f9bf5480de422fa2798c0989` on a cold worktree (no `deps/` / `_build/` at start). Alias exit 0. Status: **incomplete** — two configured gates skipped for missing host scripts.

| Step | Outcome |
|------|---------|
| `compile --warnings-as-errors` | passed (`Generated zen_websocket app`, 49 files) |
| `format --check-formatted` | passed (silent) |
| `credo --strict` | passed — 113 files, 917 mods/funs, no issues |
| `doctor --raise` | passed — 50 modules, 100% doc / moduledoc / spec coverage |
| `ex_dna --max-clones 0` | passed — no duplication, 49 files, budget 0/0 |
| `reach.check --arch --smells` | passed — architecture OK, no smells |
| `sobelow --skip --exit low` | passed — SCAN COMPLETE (no-router warning expected; not Phoenix) |
| `deps.audit.gated` | **incomplete** — advisory-mirror freshness skipped; then "No vulnerabilities found." |
| `test.json --cover --cover-threshold 90 --exclude integration --include local_network` | passed after retry — 641 passed, 0 failed, 3 flaky, 70 excluded, coverage 94.14% (threshold 90) |
| `MIX_ENV=dev mix dialyzer` | passed — PLT built from scratch; total errors 1, skipped 1 |
| `agents.check` | **incomplete** — AGENTS.md freshness skipped |

Log: `/tmp/harness-precommit-full.EUZQbX.log`. Hex `mix deps.get` also printed published advisories on transitive packages; those were not mixed into git. Repair for the unproven audit scan is task 23.

## Clean areas

- Plugin enables are gone; no stray `elixir@zenhive` / `harness@zenhive` references remain.
- No roadmap status edits of existing tasks; tasks 22 and 23 are new filings only.
- No reverts.
