# Post-Merge Audit: 40bd46a

**Date:** 2026-08-21
**Auditor:** post-merge audit agent (best-effort hygiene, fix-forward only)
**Range end / marker:** `40bd46a` — roadmap: task R051 -> done (shipped 65728bd541da)

## Range reviewed

| Task | Delivery and review commits | Surface |
|------|-----------------------------|---------|
| 10 | `8556c03` | Drop the dead pool-health `pressure_level` penalty |
| R051 | `65728bd` | Decompose `Reconnection.establish_connection/1` (dominator depth 39) |

Roadmap-only commits in the supplied range (`a6fb944`, `27fe787`, `5ea60ed`, `4eea56f`, `0f25a99`, `40bd46a`) were checked for continuity and left unchanged.

## What I reviewed

- Task 10: `PoolRouter.calculate_health/1` now scores pending requests, latency, and errors only. `get_client_metrics/1` no longer reads `pressure_level`; `pressure_to_penalty/1` and the penalty map are gone. Architecture.md and Descripex metadata match. A `:local_network` test asserts a real `Client` has no `:pressure_level` key and scores 100.
- Task R051: `establish_connection/1` is a `with` over `prepare_target/1`, `open_transport/3`, `await_transport/2`, and `upgrade_websocket/3`. Failed `await_up` still demonitors and closes Gun. Public return shape is unchanged. `docs/Architecture.md` and `docs/gun_integration.md` still describe the public function, not the old nested body.
- Hygiene scan of landed additions: no leftover `IO.inspect`/`dbg`, no `@tag :skip`, no new `# credo:disable`, no bare `TODO:`.
- RateLimiter `status/1` still returns a compatibility `pressure_level: :none`. That is the dead queue contract from task 3, not pool health, and was already documented.
- No reviewer rejections were recorded, so there is no false-rejection note.

## Findings

| # | Category | Description | Resolution |
|---|----------|-------------|------------|
| 1 | changelog-gap | `[Unreleased]` still described pool health only as “ETS owner process survives the first caller.” Task 10 dropped the unused `pressure_level` penalty; Architecture.md was updated in the delivery, CHANGELOG was not | **fixed** |

R051 is an internal split of a private helper. Public API, telemetry, and reconnection docs are unchanged, so it is not a changelog entry.

## What I fixed

- Extended the Unreleased heartbeat/pool-health note: scores are pending requests, latency, and errors; the unused `pressure_level` penalty is gone because `Client.get_state_metrics/1` never reported that key.

## Discoveries filed

None. Task 12 (tag-consistency guard) was already filed in this range. Remaining `FakeMetricsClient` use is the scoring-formula driver, now paired with a real `Client` assertion that the pressure key is absent.

## Cold check

Command: `mix deps.get` then `mix check.dispatch` in this intentionally cold worktree — **passed** (exit 0).

Witness: 523 tests passed, 0 failed, 92 excluded; coverage 76.56% (threshold 58); credo strict clean (91 files, no issues); doctor passed; ex_dna no clones; reach arch/smells clean; sobelow skip-clean; deps.audit clean; dialyzer passed (PLT built from scratch); `agents.check` OK. Also recorded in `.harness/audit.json` `cold_check`.

## Verification (audit fixes)

- Changelog only. No behaviour change.
- Cold `mix check.dispatch` is the suite witness for the already-landed code range.

## Clean areas

- Pool health no longer subtracts a term a live Client cannot produce.
- `establish_connection/1` phases keep Gun ownership and the failed-await cleanup in the Client process.
- README already listed pool scoring as pending requests, latency, and errors.
- No roadmap status edits in this audit commit; no reverts.
